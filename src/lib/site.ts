// URL canonique du site, utilisée pour le sitemap, robots.txt et les
// données structurées (JSON-LD). À mettre à jour si le domaine change
// à nouveau (ex: passage à un nom de domaine propre).
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://fasshon.vercel.app";
